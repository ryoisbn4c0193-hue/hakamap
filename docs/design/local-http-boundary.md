# ローカルHTTP境界設計

## 目的

HakamapのブラウザUIとSpring Boot間の通信を端末内に限定し、同じ端末上の
別プロセスや外部Webサイトからの意図しない操作を防止する。

## 決定事項

### ネットワーク境界

- パッケージ版のHTTPサーバーはIPv4ループバックアドレス`127.0.0.1`だけに
  バインドする。
- `0.0.0.0`および端末のLANアドレスにはバインドしない。
- 名前解決結果に依存しないよう、パッケージ版の接続先には`localhost`ではなく
  `127.0.0.1`を使用する。
- パッケージ版では固定ポートを使用せず、起動時にOSが利用可能なポートを選択する。
- 開発時は`127.0.0.1:8080`をSpring Bootの既定の接続先として使用してよい。
- Viteの開発サーバーもループバックアドレスだけにバインドする。

### UIとAPIの配信

- パッケージ版では、Reactの本番ビルドと`/api`を同じSpring Bootプロセスから配信する。
- UIとAPIを同一オリジンにし、パッケージ版でCORSを有効にしない。
- 開発時に限り、ViteからSpring Bootの`/api`へプロキシしてよい。
- 実行時に外部のWebサーバー、CDN、地図タイル、認証サービスへ依存しない。

### ローカルアクセスの認証

- `127.0.0.1`からのアクセスであることだけを認証として扱わない。
- バックエンドの起動ごとに、推測が困難な一時的な認証情報を生成する。
- ランチャーが開いたブラウザだけが、その認証情報を使用してアプリケーションの
  セッションを開始できるようにする。
- セッション開始後の認証状態は、JavaScriptから読み取れない`HttpOnly` Cookieで管理する。
- Cookieには少なくとも`SameSite=Strict`を設定する。
- 起動用の認証情報を受け取った後は、それをブラウザのアドレス欄や通常の画面URLに
  残さないよう、認証情報を含まないURLへリダイレクトする。
- 起動用の認証情報をアプリケーションログへ記録しない。
- 状態を変更するAPIには、セッション認証に加えてCSRF対策を適用する。
- OriginやHostなど、ブラウザが送信するリクエスト情報も補助的に検証する。

### CSRFトークンの受け渡し

- 起動用認証情報からHttpOnlyセッションCookieを確立した後、React起動時に呼ぶ
  `GET /api/v1/session`のレスポンスヘッダー`X-Hakamap-CSRF-Token`でCSRFトークンを返す。
- `GET /api/v1/session`は有効なHttpOnlyセッションCookie、同一オリジンのOrigin、
  および正しいHostを要求する。起動用認証情報だけ、未認証状態、または外部Originからは
  CSRFトークンを取得できない。
- ReactはトークンをタブのJavaScriptメモリだけに保持し、localStorage、sessionStorage、
  IndexedDB、Cookie、URL、およびログへ保存しない。
- 状態変更リクエストは`X-Hakamap-CSRF-Token`ヘッダーでトークンを送る。
- CSRFトークンの有効期間はセッションと同一とし、固定時刻による独立更新は行わない。
- セッションを再生成した場合は新しいCSRFトークンも生成し、以前の組み合わせを直ちに無効化する。
- 複数タブは同じセッションCookieを共有できるが、各タブが認証済みAPIからトークンを取得し、
  自身のメモリへ保持する。トークンをタブ間メッセージやWeb Storageで共有しない。
- セッションまたはCSRF失効時は変更リクエストを自動再送せず、新しい起動用認証から
  セッションを確立してスナップショットを再取得する。

### 二重起動との連携

- `%LOCALAPPDATA%\Hakamap\runtime\application.lock`をOSの排他ファイルロックとして使用し、
  最初のプロセスだけがバックエンドを開始する。
- 起動中のPID、インスタンスUUID、ポート番号、起動日時、および256ビット相当のランダムな
  制御トークンを、現在のWindowsユーザーだけがアクセスできる`runtime\instance.json`へ保存する。
- 2回目のプロセスはPID、インスタンスUUID、制御トークン、およびHakamap固有応答を確認してから、
  既存プロセスへ画面を開く要求を送信する。
- 既存プロセスが新しい一時的な起動用認証情報を生成してブラウザを開き、2回目のプロセスへ
  通常画面のURLやブラウザ用認証情報を返さない。
- 最初のプロセスが準備中の場合は最大10秒間再試行し、確認できない場合もバックエンドを重複起動しない。
- 排他ロックを取得できる場合は残存した起動情報を無効とみなし、削除して新規起動する。
- 正常終了時は起動情報を削除してから排他ロックを解放する。
- PIDまたは起動情報だけを根拠として既存プロセスを信頼しない。
- 起動情報、ポート番号、制御トークン、および一時認証情報をログへ記録しない。
- 起動用認証情報やセッションはバックエンドプロセスの終了時に無効になるものとし、
  永続的なユーザー認証情報として保存しない。

## 開発環境

- 開発時もSpring BootとViteをループバックアドレスだけにバインドする。
- Viteから`/api`へプロキシし、アプリケーションコードに開発用API URLを埋め込まない。
- 開発用の認証省略が必要になった場合は、明示的な開発プロファイルだけに限定し、
  パッケージ版で有効にならないことをテストする。

## 検証要件

- サーバーソケットが`127.0.0.1`だけで待ち受けていることを統合テストまたは
  パッケージ試験で確認する。
- LAN上の別端末から接続できないことをWindowsパッケージ試験で確認する。
- 認証情報がないリクエストでは、非公開データの取得と状態変更ができないことを確認する。
- 外部オリジンを想定した状態変更リクエストが拒否されることを確認する。
- 未認証または外部OriginからCSRFトークンを取得できず、認証済みレスポンスヘッダーから
  取得したトークンだけで状態変更できることを確認する。
- セッション再生成後に旧CSRFトークンが拒否され、複数タブが永続ストレージを使わず
  各タブのメモリで同じセッション用トークンを利用できることを確認する。
- 2回目のランチャー起動で新しいバックエンドが作られず、既存の画面が開くことを確認する。
- 最初のプロセスが準備中でも10秒以内に利用可能になれば既存画面が開き、利用できない場合に
  重複バックエンドが起動しないことを確認する。
- 不正なPID、インスタンスUUID、または制御トークンによる画面起動要求が拒否されることを確認する。
- 古い起動情報が残った状態から正常に復旧できることを確認する。

## 採用しない方式

- `0.0.0.0`へのバインド
- 制限のないCORS
- ループバック接続であることだけを根拠とする無認証API
- 固定された認証トークンの永続利用
- 認証情報をURLへ残したまま通常画面を利用する方式
