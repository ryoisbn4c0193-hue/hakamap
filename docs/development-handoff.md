# 開発引き継ぎメモ

最終更新日: 2026-08-11

## 現在の目的

レビュー合格済みの要件・基本設計・詳細設計に従い、Hakamap MVPを
`docs/implementation-plan.md`のPhase単位で実装する。Phase 6の編集コマンドAPIと
自動復旧の実行時接続と復旧破棄の再試行対応まで完了し、外部レビューに合格した。
Phase 8のPixiJS地図編集まで外部レビューに合格し、Phase 9とPhase 10の製品機能を完了した。
Phase 10のデータ安全性、排他制御、アーカイブ防御、および協調キャンセルまで
外部レビューに合格した。Phase 11のWindows配布・性能・障害検証を進めており、
配布・検証スクリプトとLinux上の最小ランタイム検証まで完了した。
- 当時のコミット`aa92223`に対する再レビューは合格となり、重要な矛盾が解消され、
  MVP、将来拡張、および未決事項の境界が明確であると確認された。
- 基本アーキテクチャとして、React、TypeScript、PixiJS、Java 21、Spring Boot、および
  既定ブラウザによる構成を採用した。Tauri、Electron、およびJavaFXは採用しない。
- 永続化はプロジェクトフォルダ内のJSONとアセットファイルを採用し、H2、Spring Data JPA、
  およびFlywayは採用しない。

## 今回完了した作業

- Phase 1として、Project、Area、Grave、Person、Assetの型付きUUID、文字列値、
  地図座標・寸法・長方形、回転、倍率、表示順、および各種列挙を実装した。
- 文字列の前後Unicode空白除去、コードポイント単位上限、改行・制御文字検証、
  NFKC、英字大小、全Unicode空白、ひらがな・カタカナを統一する比較キーを実装した。
- 地図数値を`BigDecimal`の小数第3位へ丸め、長方形の中心、閉領域包含、面積を持つ交差、
  辺・角接触、および上下重なりを判定する値オブジェクトを実装した。
- Project集約と構成エンティティを実装し、種類横断UUID、参照、表示順、エリア数・名称・色、
  添付上限、エリア・墓所重複、および墓所業務キーを候補状態全体で検証するようにした。
- 墓所中心点とエリア表示順による所属、完成・未完成理由、および未割当・未採番・
  エリア範囲外警告の導出を実装した。
- 単体追加、行列・範囲埋め生成、複数移動、情報を引き継がない複数コピー、
  関連データを含む削除、および一括採番のドメイン処理を実装した。
- 一括操作は変更後候補を検証してから状態を一度に切り替え、1件でも違反があれば
  元のProject状態を維持するようにした。
- 一括採番の視覚行を、中心座標の安定順、基準墓所固定、上下重なり50%以上、
  行内左から右・行間上から下の確定ルールで実装した。
- Unicode、丸め、接触、共有境界、50%行判定、重複、参照、表示順、上限、完成状態、
  コピー内容、および一括操作の原子性を単体テストへ追加した。
- Phase 0として、H2、Spring Data JPA、およびFlywayの本番・テスト依存を除去した。
- NetworkNT JSON Schema Validator 3.0.6を導入し、Project、Catalog、Recoveryの
  Draft 2020-12 Schemaをclasspathからコンパイル・検証する基盤とスモークテストを追加した。
- MUI、Emotion、TanStack Query、Zustand、Zod、PixiJSを導入し、製品テーマ、
  Provider、エラー境界、UI状態Store、PixiJSアダプター、および最小3領域画面を追加した。
- 開発用Viteサーバーを`127.0.0.1`へ限定し、`/api`を同アドレスのSpring Bootへ
  プロキシする設定を追加した。
- Mavenから固定Node.js 24.18.0・pnpm 11.15.1でフロントエンド標準検査とビルドを実行し、
  React成果物を実行可能JARへ同梱する統合ビルドを追加した。
- 生成JARを`127.0.0.1`で起動し、画面のHTTP 200と未定義APIのHTTP 404を
  同一オリジンで確認した。
- 空だったルート`README.md`へ、製品概要、MVP、アーキテクチャ、開発コマンド、データ取扱い、
  文書への入口、および現在の設計状況を記載した。
- `AGENTS.md`から重複する製品・設計詳細をREADMEと`docs`へ移し、AI・開発者向けの変更制約、
  コーディング、検査、引継ぎ、および使用言語の規約へ整理した。
- `AGENTS.md`に残っていたH2前提や古い未決事項の重複を除き、JSON永続化の確定設計と
  文書の正本を明示した。
- 300ミリ秒を超える保存・読み込み等でモーダル進捗を表示し、工程、算出可能な進捗率、
  またはスピナーを示すことを定めた。
- 手動保存と読み込みはキャンセル不可、バックアップ、インポート、エクスポート、復元は
  確定処理前までキャンセル可能とし、処理中の編集、二重実行、切り替え、終了を止めることを定めた。
- 端末単位の`application.lock`を`FileChannel.tryLock()`で保持し、起動情報へPID、インスタンスUUID、
  ポート、日時、ランダム制御トークンを現在のWindowsユーザーだけが読める状態で保存することを定めた。
- 二重起動時はPID、UUID、トークン、Hakamap応答を確認して既存プロセス側でブラウザを開き、
  準備中は最大10秒再試行し、残存情報だけなら清掃して新規起動することを定めた。
- 起動ロック取得直後に`unclean-exit.marker`を作り、インスタンスUUID、アプリ版、起動日時、
  5秒ごとの生存日時だけを保持して正常終了時に削除することを定めた。
- マーカー残存時は異常終了として通知するがプロジェクトを自動変更せず、マーカーの有無に
  かかわらず復旧JSONと一時アセットを毎回検査することを定めた。
- 通常自動バックアップを`backup/automatic/backup-<UTC日時>.zip`として一時作成・検証後に確定し、
  新世代の成功後だけ4世代目以降を削除することを定めた。
- MVPでは手動バックアップを設けず任意コピーには`.hakamap`エクスポートを使い、
  バックアップ中は編集、保存、切り替え、終了を停止することを定めた。
- バックアップ一覧には正常な最大3世代を新しい順で表示し、ローカル作成日時、容量、
  アプリバージョン、プロジェクト名、復元操作を示すことを定めた。
- 破損等は復元候補から除外して通知するが自動削除せず、MVPでは手動削除を設けず、
  内部ファイル名、絶対パス、内部エラー詳細を表示しないことを定めた。
- 上書き復元前に`backup/pre-restore/pre-restore-<UTC日時>.zip`へ現在の保存済み状態を必ず退避し、
  検証失敗時は復元せず、通常バックアップと別に最新3世代保持することを定めた。
- 復元前退避も別グループから復元可能とし、復元後はバックアップ内のプロジェクト情報を使い、
  保存場所・デフォルト設定は維持、履歴は破棄、リビジョンは0として再読み込みすることを定めた。
- ズームを10～800%、1操作約20%とし、ホイール・ピンチ・ボタン・`+`／`-`／`0`で操作し、
  背景・エリア・墓所を対象とする全体表示と初回表示を定めた。
- パンは中ボタンドラッグまたはSpace＋左ドラッグとし、範囲制限なし、選択や保存座標を変えず、
  テキスト入力中はキーボードショートカットを無効にすることを定めた。
- Shiftは選択追加に使わず、矢印の10単位移動、ドラッグ移動の水平・垂直制約、サイズ変更と
  背景拡縮の縦横比維持に使用し、ドラッグ途中の押下・解除にも追従することを定めた。
- エリア色を青、緑、オレンジ、紫、赤、グレーの固定IDとし、初期青、境界線2px相当、
  同色20%塗りつぶし、選択時は別の強調線、色だけに依存しない表示とすることを定めた。
- MVPでは任意色、透明度、線幅変更を設けず、不明IDを拒否し、将来の表示色調整でも
  保存済みIDを維持することを定めた。
- PNG・JPEG・WebP背景は元画像だけ、PDFは変換後PNGだけを保存し、二重保存しないことを定めた。
- 背景透明度を100%固定、実寸縮尺とアプリ内縮小をMVP対象外とし、上限超過時は理由と
  許容上限を示して拒否し、これらを将来拡張候補へ整理した。
- Project、Catalog、RecoveryのSchema v1をMVP初期形式とし旧版変換は持たず、v2以降の追加時に
  Schemaと隣接版Migratorを同時追加することを定めた。
- 解消済みの空の未決事項見出しを削除し、手動バックアップをMVP外の将来候補へ整理した。
- 要件、基本設計、ADR、READMEを横断確認し、MVPと将来拡張の境界、未決事項、文書間の
  整合性を確認して基本設計完了と判定した。
- 基本設計レビューで、背景描画上限、所属エリア判定、色の一意性、NAS排他、
  自動バックアップ契機の5点が修正必須として指摘された。
- 高解像度背景は1024px以下のタイル階層を端末キャッシュへ生成し、表示範囲周辺だけを
  PixiJSへ読み込む方式とした。原本だけをプロジェクトへ保存し、タイルはデータ移送へ含めない。
- GPU最大テクスチャサイズを確認し、最大背景と1GB目標を最低環境のWindows実機で試験し、
  満たせない場合はリリース前に上限または目標を見直すことを定めた。
- 墓所の中心点を含むエリアへ所属させ、共有境界上では`displayOrder`最小を使用し、候補なしは
  未割当、墓所全体が収まらない場合は保存可能な警告とすることを定めた。
- 所属と警告を各地図操作・Undo／Redo・エリア変更時に再計算し、現在の所属UUIDを業務キーへ
  使用するがJSONへは保存しないことを定めた。
- エリア色を状態色と重複しない12プリセットへ拡張し、プロジェクト内で一意、エリア上限12件、
  未使用色の自動設定、削除後の色再利用、使用中選択肢の無効化を定めた。
- 選択・警告・エラー・無効の状態色を固定して前面表示し、将来の色追加では既存IDを維持して
  Schema、検証、UI、エリア上限を同時更新することを定めた。
- ネットワーク上の保存先は、機能検査に合格したWindows 11のSMB 3.x共有に限定し、
  単一端末・単一Windowsユーザーで利用することを定めた。
- 新規作成・再関連付け時に読み書き、SHA-256、削除、別Javaプロセスとのロック競合、
  原子的置換を検査し、失敗時は編集を拒否して非原子的保存へフォールバックしないことを定めた。
- SMB共有では、別端末とのロック競合、LAN切断、共有サーバー再起動、保存中の切断、
  再接続後の整合性をWindows 11実機で試験することを定めた。
- 自動バックアップはデータ変更を含む手動保存成功直後に判定し、Windowsのローカル日付で
  1日1回、新規プロジェクトでは最初の保存後に作成することを定めた。
- バックアップ失敗時も手動保存は成功扱いとし、警告して次の手動保存成功後に再試行する。
  起動・定時・終了時には開始せず、更新日時、リビジョン、Undo／Redo履歴を変更しない。
- ブラウザを閉じてもバックエンドは終了させず、デスクトップまたはスタートメニューの
  ショートカット再実行で既存バックエンドの画面を開き直すことを定めた。
- 画面内に「Hakamapを終了」とヘルプを設け、初回起動時にも終了・再表示方法を案内する。
  MVPではタスクトレイへ常駐しない。
- エリア名と管理番号は入力表記を保存しつつ、NFKC、英字大小、Unicode空白、
  ひらがな・カタカナを統一した導出キーで一意性を判定することを定めた。
- 座標と寸法は小数第3位へ丸めた候補値で幾何判定し、丸め後の寸法0以下または重なりは
  コマンド全体を拒否することを定めた。
- 墓所の完成状態と警告を分離し、未割当・未採番は複数理由を持つ情報未完成、
  エリアからのはみ出しは完成状態へ影響しない保存可能な警告とすることを定めた。
- エリアの非表示は所属と業務キーへ影響させず、削除とUndo／Redo時は残存する候補状態から
  所属、完成状態、および警告を再計算することを定めた。
- 墓所コピーは幅・高さ・内部回転角度だけを引き継ぎ、管理番号、墓所名、備考、人物、
  添付を持たない新規UUIDの墓所を作ることを定めた。
- 一括コピーは元の位置関係と同じ移動量を使用して全件成功または全件失敗とし、
  コピー元のデータと更新日時を変更しないことを定めた。
- 単体墓所はドラッグ、一括作成は行列・寸法・間隔・左上位置、範囲埋めは長方形範囲と
  寸法・間隔を入力し、左から右、上から下へ生成することを定めた。
- 範囲へ完全に収まる墓所だけを作り、生成墓所は新規UUID、回転0、同一更新日時、
  その他情報なしとし、重なり等は全件拒否、未割当・はみ出しは警告許可と定めた。
- 一括採番は接頭辞、開始番号、1～10桁、接尾辞、増分1で生成し、視覚的な行を左から右、
  上から下へ並べて割当予定をプレビューすることを定めた。
- 採番済み・未割当墓所を含む場合や文字数・業務キー違反時は全件拒否し、複数エリアでは
  番号を継続し、プレビューの手動並べ替えはMVP外と定めた。
- JSON Schema検証にはJackson 3・Java 17以上・Draft 2020-12対応の
  `com.networknt:json-schema-validator:3.0.6`を固定採用し、外部Schema取得を無効にして
  Apache License 2.0の表示を配布物へ含めることを定めた。
- ドラッグは確定まで、フォームは保存・適用、矢印キー連続入力はキーを離すまでを
  1履歴とし、実質的な変更がない操作は履歴・リビジョン・日時を変更しないことを定めた。
- 未保存判定は決定的な保存モデルの状態フィンガープリントで行い、Redoは元コマンドを
  再実行せず最初に確定したUUID・日時を含む変更後差分を再適用することを定めた。
- 未割当・はみ出し警告が新しく発生する場合だけ反映前に確認し、未採番、既存警告の継続、
  Undo／Redo、保存では再確認しないことを定めた。
- 一括警告は理由別件数をまとめ、候補をサーバーのメモリへ5分間だけ保持する単一使用トークンで
  同一リビジョンに対して確定し、キャンセル・期限切れ・競合時は反映しないことを定めた。
- 変更なしの保存はJSON・日時・リビジョン・履歴・バックアップを変更しないno-opとし、
  保存失敗時は旧正式データと未保存編集を維持して再試行・別保存・キャンセルを提示する。
- Project保存は検証済み一時JSONの原子的置換を確定点とし、確定時だけ更新日時を反映する。
  確定後の不要ファイル清掃失敗は保存成功を維持して警告し、次回起動・保存で再試行する。
- 自動バックアップは手動保存確定後の別トランザクションとし、失敗しても保存成功と
  既存3世代を維持することを定めた。
- 自動復旧候補は保存日時と復旧日時を表示し、適用時は正式ファイルを上書きせず、
  未保存状態、revision 0、空の履歴として開くことを定めた。
- 復旧後は手動保存または変更破棄を選べ、Recovery・Project・一時アセットの全検証に
  成功しない限り部分復旧せず、正式保存データを維持することを定めた。
- バックアップ復元は検証済みの復元前退避を必須とし、不変UUIDの必要アセットを先に配置して
  `project.json`の原子的置換を確定点とすることを定めた。
- 原子的置換中の切断で結果を検証できない場合は自動再試行せず、再接続後に新旧JSONの
  SHA-256で確定状態を判定し、どちらでもなければ元保存先へ上書きしないことを定めた。
- APIを`/api/v1/projects/{projectId}`境界とし、初回は全スナップショット、変更成功時は
  確定差分・導出状態・最新revisionだけを返すことを定めた。
- 長時間処理はセッションに紐づくOperation IDを返し、約500ミリ秒間隔で進捗取得、
  確定段階後はキャンセル不可、未取得結果は10分で破棄することを定めた。
- 一括Problem Detailsは個人情報を含まない違反を最大100件と総件数で返し、
  API v1と保存Schemaのバージョンを独立管理することを定めた。
- 検索語は最大200文字、結果は総件数付きの不透明カーソルで200件ずつ取得し、
  revision変化時は先頭から再検索することを定めた。
- API JSON 10MB、ネスト50、文字列1MB、数値64文字、Project 100MB、Catalog 10MB、
  Recovery 110MBのパーサー防御値を定め、MVPの画像配信ではRangeを使用しない。
- フロントエンドではJavaの確定状態をTanStack Query、選択やviewportなどのUI状態を
  Zustand、入力途中と地図プレビューを一時状態として分離することを定めた。
- 未確定フォームがある選択変更では破棄またはキャンセルを確認し、リビジョン競合時は
  地図操作を自動再実行せず、フォーム入力だけを確認可能な状態で保持することを定めた。
- PixiJSの描画順を背景、エリア、墓所、操作オーバーレイとし、通常時は墓所を優先して
  エリアを専用編集モードで操作することを定めた。
- 非表示エリアを描画・選択対象外とし、矩形選択では辺・角の接触を含めて墓所と
  少しでも交差すれば選択することを定めた。
- スナップ検出距離をズームに依存しない画面上8pxとし、最小補正量、角、辺、墓所、
  エリア、表示順、UUIDの順で候補を一意に決めることを定めた。
- 一括採番では高さが小さい墓所を基準に上下方向が50%以上重なる墓所を同じ視覚的な行とし、
  行内を中心X、行を中心Y、最終同値をUUIDで安定して並べることを定めた。
- テストデータは架空データと生成fixtureだけを使用し、小規模10墓所、標準1,000墓所、
  上限5,000墓所のデータ規模を目的別に使用することを定めた。
- 日常検査では単体・結合と標準規模までを扱い、上限データ、最大背景、NAS障害、
  性能、およびパッケージをWindows重負荷検査として分離することを定めた。
- 詳細設計レビューは完成度約90%の条件付きOKとなり、実装開始前の修正必須4点として
  人物取得方式、Operation API、ファイル選択API、およびCSRF取得方式が指摘された。
- 初回スナップショットから人物全件を除外し、選択墓所の人物を100件ずつ取得する専用API、
  仮想スクロール、人物変更差分、および墓所単位キャッシュを定めた。
- 長時間処理をProject配下の保存・エクスポート・バックアップ復元、Catalog配下の
  インポート、ごみ箱Projectの復元へ分離し、DTO、結果、キャンセル段階を定めた。
- ファイル選択開始APIへ単一・複数・フォルダ、用途列挙、キャンセル、多重起動、
  5分期限、単一使用、および明示無効化を定めた。
- CSRFトークンを認証済み`GET /api/v1/session`のレスポンスヘッダーから取得し、
  Reactのメモリだけへ保持してセッションと同時に更新することを定めた。
- Schema検証後のJava整合性検証を、UUID全体一意性、参照、表示順、色、重なり、
  添付上限、アセットパス・拡張子・MIME・完全性を含む表として明文化した。
- 追加レビューでは前回指摘がすべて解消済みと確認され、正式合格前の補完事項として
  バックアップ一覧APIとCSRF取得APIのキャッシュ・Origin条件が指摘された。
- バックアップ一覧APIへ不透明`backupId`、種別、日時、容量、アプリ版、Project名、
  復元可否、安全な理由分類、および差し替え検知用`backupVersion`を定めた。
- バックアップ復元要求へ`expectedRevision`、未保存変更なしの確認、および
  `backupVersion`を追加し、復元開始直前に実体と完全性を再検証することを定めた。
- `GET /api/v1/session`ではOriginがあれば完全一致、なければHost、セッションCookie、
  Fetch MetadataまたはRefererで同一オリジンを判定し、`Cache-Control: no-store`を
  必須とすることを定めた。
- 詳細設計の追加2点もレビューで承認され、詳細設計は正式合格となった。
- 現在のコードは初期雛形であり、バックエンドに不採用のH2・JPA・Flyway依存、
  フロントエンドに製品用UI・状態管理・PixiJS未導入という設計との差分があることを確認した。
- 実装をビルド基盤、ドメイン、JSON保存、保存トランザクション、HTTP保護、Project管理、
  編集API、React、PixiJS、検索・添付、バックアップ・入出力、Windows検証の12段階に分けた。
- 実装はPhase単位で標準検査と文書更新まで完了してからコミットし、Phase途中では
  原則コミットしない運用とした。
- Phaseコミットは通常ローカルだけに保持し、外部ChatGPTレビューの節目でプッシュする。
  プッシュ後は`HEAD`と`origin/main`を照合し、レビュー対象コミットハッシュを利用者へ伝える。
- 外部ChatGPTレビューはPhase 3、6、8、10、11の完了時を基本とし、重大リスクが
  見つかった場合は途中でも実施を提案する。
- 背景差し替えでは現在の位置・回転・X/Y倍率を引き継いでプレビューし、画像サイズ差を
  自動補正せず、差し替え・削除でエリアと墓所の座標を変更しないことを定めた。
- 複数添付追加は全ファイルを1コマンドとし、1件でも形式・容量・画素・参照・件数上限の
  エラーがあれば全件を追加しないことを定めた。
- プロジェクトの作成、デフォルト設定、起動時表示、切り替え、削除、および復元を定めた。
- 背景なしでのプロジェクト作成と、作成後の背景追加・変更を定めた。
- 複数ページPDFを背景として指定した場合は業務エラーとする仕様へ変更した。
- 墓所の一括作成、連続生成、複数選択、一括操作、重なり、コピー、および採番を定めた。
- MVPのエリアを長方形とし、墓所の所属を位置から自動判定する仕様へ変更した。
- 墓所、エリア、人物、添付ファイルはごみ箱を使わず即時削除し、Undo履歴中だけ
  復元できる仕様へ変更した。プロジェクト削除だけはごみ箱を使用する。
- 添付ファイルは墓所だけに紐づけ、1墓所20件までとする仕様へ変更した。
- 地図編集画面の3領域、プロパティパネル、選択操作、キーボード操作を定めた。
- 手動保存、未保存変更の確認、および最大100操作のUndo・Redoを定めた。
- 印刷、PDF出力、画像出力、およびプロジェクトのインポート・エクスポートを定めた。
- Windows 11の最低・推奨環境、5,000墓所を前提とする性能目標を定めた。
- 自動復旧、ログ、エラー処理、およびセキュリティ要件を定めた。
- プロジェクトの保存先を利用者が自由に指定する仕様へ変更した。
- 自動バックアップをプロジェクトの`backup`フォルダへ1日1回、3世代保存する仕様へ変更した。
- プロジェクトをフォルダ単位で管理し、構造化データを`project.json`、背景と添付を
  アセットファイルとして保存する方式を定めた。
- 編集状態はJavaバックエンドのメモリ上、自動復旧は正式保存と分離したJSON
  スナップショットとして管理する方針を定めた。
- `project.json`はUTF-8の整形済みJSONとし、閲覧可能だが手動編集はサポート対象外、
  読み込み時に構造とデータ整合性を検証する方針を定めた。
- 古いJSONスキーマは自動変換して変換前にプロジェクト一式を退避し、新しいスキーマは
  更新せず拒否すること、変換失敗時は元データを変更しないことを定めた。
- スキーマ変換前の退避は`backup/schema-migration/`の日時付きZIPとし、`project.json`と
  `assets`だけを含め、通常自動バックアップとは別管理することを定めた。
- スキーマ変換前ZIPは最新3世代を保持し、新規ZIPの検証成功後にだけ古い世代を削除し、
  失敗時は既存の退避データを維持することを定めた。
- JSON内ではプロジェクト、背景、エリア、墓所、人物、およびアセットをトップレベルで
  独立して保持し、UUID参照で関連付け、表示順は明示値で保持することを定めた。
- 未保存の新規アセットはアプリ管理領域でステージングし、保存時に正式反映、変更破棄時に
  削除すること、削除対象の正式アセットは保存完了まで保持することを定めた。
- 自動復旧JSONとステージング済みアセットを関連付け、不要な一時領域を次回起動時に
  清掃する方針を定めた。
- 手動保存では新規アセット、検証済み一時JSON、JSON置換、旧アセット削除の順に処理し、
  JSON置換を確定点として、途中失敗時は旧データを維持する方針を定めた。
- アセットの正式名はUUIDと許可済み拡張子とし、元ファイル名はJSONに保持すること、
  未参照のUUID形式ファイルだけを自動清掃し、不明なファイルは削除せず警告することを定めた。
- アセットメタデータにUUID、元ファイル名、相対パス、MIMEタイプ、サイズ、SHA-256、
  追加日時を保持し、読み込みやデータ移送時に検証することを定めた。
- システム日時はUTCのISO 8601形式でミリ秒まで保存して画面ではローカル時刻へ変換し、
  日付だけの業務項目はタイムゾーンなしのISO日付として保存することを定めた。
- UUIDはJavaバックエンドがVersion 4で生成し、作成後は不変、コピー時は新規発行、
  インポート時は維持し、重複時はデータを変更せず拒否することを定めた。
- プロジェクトの作成日時は不変、更新日時は変更の手動保存成功時だけ更新し、閲覧、
  バックアップ、変更破棄では更新しないこと、インポート時は両日時を維持することを定めた。
- プロジェクトは`.hakamap.lock`で排他的にロックし、取得失敗時や安全な排他を提供しない
  NASでは編集を拒否すること、MVPでは読み取り専用オープンを設けないことを定めた。
- 保存先の切断またはロック喪失時は編集と元保存先への保存を止め、状態をメモリとローカル
  復旧領域へ保持し、再接続、別保存、破棄を選べることを定めた。
- 再接続時はロックを再取得し、基準JSONのSHA-256と一致する場合だけ編集を再開し、
  外部変更がある場合は元保存先へ上書きしないことを定めた。
- 切断後の別保存は新しいプロジェクトUUID、名称、作成日時、更新日時を設定し、配下データの
  UUIDは維持すること、元プロジェクトとデフォルト設定は変更しないことを定めた。
- 保存先とロックは5秒間隔で監視し、通常監視は連続2回失敗、変更・保存系操作の直前は
  1回の失敗で停止し、利用者の明示操作があるまで再開しないことを定めた。
- `.hakamap.lock`はJavaの`FileChannel.tryLock()`で取得・保持し、起動識別子、プロセスID、
  アプリバージョン、起動日時、生存日時だけを記録することを定めた。
- 5秒ごとに`FileLock.isValid()`、生存日時の更新、`force(true)`を実行し、正常終了時は
  ロック解放後にファイルを削除し、残存ファイルだけでは使用中と判定しないことを定めた。
- JSON、API、およびJavaのプロパティ名は`camelCase`、欠損する任意値はプロパティ省略、
  空コレクションは`[]`、列挙値は
  英語の固定文字列とし、UUID・日時・日付・相対パスの形式をSchemaで検証することを定めた。
- 各JSONオブジェクトは未定義プロパティを拒否し、読み込み時と保存時にJSON Schema検証後、
  Javaバックエンドで参照関係などの業務整合性を検証することを定めた。
- 保存プロパティの追加・削除・変更では必ずスキーマバージョンを1つ上げ、保存構造を
  変えない変更では上げず、新しい任意項目は既存データで省略したまま変換可能と定めた。
- 地図座標は左上原点、右X正、下Y正の保存上の論理単位とし、画面ピクセルおよびズーム率から
  分離する。位置は負数可、寸法と背景倍率は
  正数、回転は時計回りの0度以上360度未満、小数第3位へ丸めて保存することを定めた。
- 背景倍率の初期値をX・Yとも1とし、非有限数を拒否して正規化後の座標で重なり・接触を
  判定することを定めた。
- `project.json`の最上位を`schemaVersion`、`project`、任意の単一`background`、`areas`、
  `graves`、`people`、`assets`とし、背景実体をアセットUUIDで参照することを定めた。
- プロジェクトにはUUID、名称、作成・更新日時だけを保持し、絶対パス、アプリバージョン、
  端末設定、ズーム、パン、選択状態を含めないことを定めた。
- エリアにはUUID、名称、長方形座標、回転角度、色プリセット、表示状態、0始まりの
  `displayOrder`を保持し、形状種別、個別色コード、エリア単位の日時は保存しない。
- 墓所へ`areaId`を保存せず、所属エリアを正規化済みの位置から自動判定することを定めた。
- 墓所にはUUID、任意の管理番号・名称・備考、長方形、回転角度、更新日時を保持し、形状種別、
  作成日時、所属エリア、完成・未割当状態は保存しないことを定めた。
- 人物と添付から`graveId`で墓所を参照し、その追加・変更・削除・並べ替えでも墓所更新日時を
  更新すること、および墓所配列の位置に業務上の意味を持たせないことを定めた。
- 人物にはUUID、墓所UUID、任意の氏名・戒名、登録・更新日時、墓所内表示順を保持し、
  氏名・戒名の一方を必須、表示順を0始まりの連番とすることを定めた。
- 人物の論理削除フラグを保存せず、墓所削除時の関連人物とUndo／Redoデータは編集中の
  メモリで扱い、正式JSONへ残さないことを定めた。
- アセットを背景・添付共通の配列で管理し、UUID、種類、元ファイル名、相対パス、元・保存後MIME、
  保存後サイズ・SHA-256、登録日時を共通項目とすることを定めた。
- 添付だけに墓所UUID、表示名、任意説明、更新日時、墓所内表示順を持たせ、Schemaの`oneOf`で
  背景・添付に不要な項目を拒否することを定めた。
- PDFはPNGへ変換して元PDFを永続保存せず、元ファイル名と`sourceMediaType`でPDF由来を保持することを定めた。
- JSON SchemaはDraft 2020-12とし、`backend/src/main/resources/json-schema`配下へ
  project、catalog、recovery別・バージョン別の自己完結ファイルとして置くことを定めた。
- Schemaの`$id`はURN、実行時の外部参照は禁止、旧版も同梱し、Javaの隣接版Migratorで
  変換元Schema、変換、変換先Schema、業務整合性の順に検証することを定めた。
- 端末ごとのプロジェクト一覧とデフォルト設定を`catalog.json`へ保存し、絶対パスを
  プロジェクト本体から分離することを定めた。
- 未接続のプロジェクトも一覧に残し、移動時の再関連付け、データを消さない登録解除、
  およびプロジェクト本体の削除を別操作として扱うことを定めた。
- `catalog.json`はスキーマバージョン付きの整形済みJSONとして安全に置換し、直前の正常な
  内容を`.bak`へ1世代保持することを定めた。
- カタログには任意のデフォルトUUIDとプロジェクト配列を保持し、各項目へUUID、現在の絶対パス、
  最終確認済み名称・日時、`active`／`trashed`状態を保存することを定めた。
- `trashed`だけに削除前パスを必須とし、接続可否と配列順は保存せず、デフォルトは存在する
  `active`プロジェクトだけを参照できることを定めた。
- カタログを復旧できない場合も本体データを変更せず、既存の`project.json`を再登録して
  UUIDと日時を維持したまま一覧を再構築できることを定めた。
- 同一プロジェクトUUIDの重複登録を拒否し、既存保存先が利用不能な場合は確認付きで
  再関連付けすることを定めた。
- コピーを別プロジェクトとして取り込む場合はプロジェクトUUID、名称、日時だけを新規設定し、
  配下UUIDを維持して別保存先へコピーし、元データとデフォルト設定を変更しないことを定めた。
- プロジェクト削除では元と同じ親フォルダの`.hakamap-trash/<project-uuid>/`へ一式を移動し、
  カタログへ元の場所を保持し、安全に移動できない場合は削除しないことを定めた。
- 復元時に元の場所が使えなければ別の復元先を指定し、完全削除時だけ物理削除し、
  削除失敗時はごみ箱内に残して再試行できることを定めた。
- 開いているプロジェクトに未保存変更がある場合は、保存して削除、破棄して削除、キャンセルを
  選択でき、移動前に閉じてロックを解放することを定めた。
- 移動成功後はプロジェクト選択画面へ戻してUndo／Redo履歴を破棄し、失敗時は可能なら
  元の場所で再度開き、開けなくても削除済みとして扱わないことを定めた。
- 自動復旧は未保存変更がある間だけ30秒ごと、プロジェクトごとに最新1世代の復旧JSONを
  安全に置換して保持することを定めた。
- 復旧ファイル名を`<project UUID>.recovery.json`とし、復旧形式・アプリ版、プロジェクトUUID、
  作成日時、基準JSONハッシュ、完全なプロジェクトスナップショット、一時アセット一覧を保持することを定めた。
- 一時アセットは`temp`基準の相対パス・サイズ・SHA-256で参照し、Undo履歴・画面状態を含めず、
  復旧後のリビジョンを0から開始してProject Schemaでも検証することを定めた。
- 手動保存または変更破棄の成功後に復旧JSONと不要な一時アセットを削除し、破損した
  復旧データは適用せず正式保存データを変更しないことを定めた。
- 復旧JSONに対象UUID、バージョン、作成日時、基準JSONと一時アセットのSHA-256を保持し、
  基準一致時だけ元プロジェクトへ適用することを定めた。
- 基準不一致時は元データを上書きせず、別プロジェクトとしての復旧または復旧データの
  破棄を選択できることを定めた。
- 編集状態の正はJavaバックエンドの`ProjectAggregate`とし、プロジェクトを開くたびに
  リビジョンを0から開始することを定めた。
- データ変更とUndo／Redoは`expectedRevision`一致時だけ実行してリビジョンを増やし、
  表示操作は対象外とし、複数タブからの古い変更を拒否することを定めた。
- リビジョン競合時は入力値を一時保持し、最新状態を取得したうえで、入力破棄、確認後の
  再適用、またはコピー可能な退避を選択できることを定めた。
- 競合した地図操作や一括操作は自動再実行せず、再適用時も最新リビジョンで再検証する。
- Undo／Redoは1回の確定操作を差分コマンド1件として最大100件メモリ保持し、一括操作も
  全体で1件、失敗操作は履歴対象外とすることを定めた。
- 手動保存では履歴を維持し、プロジェクト終了・切り替え・アプリ終了で破棄すること、
  履歴が参照する削除済みアセットはUndo不能になるまで保持することを定めた。
- 履歴タブには操作日時、個人情報を含まない説明、適用状態、および保存位置を最大100件、
  新しい順に表示することを定めた。
- 任意時点へのジャンプ、履歴の手動削除、保存操作のUndo履歴化、および編集履歴の
  運用ログ出力は行わないことを定めた。
- MVPのファイル／フォルダ選択はSwing `JFileChooser`をEDT上で表示し、絶対パスを
  Java側だけに保持してReactへ一時選択IDを返すことを定めた。
- 選択ダイアログの同時表示と操作の二重実行を防ぎ、キャンセルは選択なしとして扱い、
  将来はJNA経由のWindows `IFileDialog`へ差し替え可能にすることを定めた。
- 一時選択IDはセッションと用途に紐づくランダムUUID、有効期限5分、正常利用1回限りとし、
  使用時に対象を再検証することを定めた。
- キャンセル、プロジェクト終了、アプリ終了時にもIDを無効化し、IDと絶対パスをログへ
  記録しないことを定めた。
- プロジェクト、カタログ、復旧JSONはそれぞれ独立した整数のスキーマバージョンを持ち、
  初期値1、非互換変更時のみ1ずつ増加、アプリバージョンとは非連動とすることを定めた。
- 各版のJSON Schemaと隣接バージョン間の変換をリポジトリで管理し、複数世代は順番に
  変換して各段階で検証することを定めた。
- Windows配布は`jlink`で最小化したJava 21ランタイムを同梱し、`jpackage`でユーザー単位の
  `.exe`インストーラーを作成することを定めた。
- スタートメニューとデスクトップへショートカットを作成し、MVPは自動更新なしで新版
  インストーラーによる上書き更新とすることを定めた。
- 更新、再インストール、アンインストールではユーザーデータを自動削除せず、削除には
  別の明示操作を必要とすることを定めた。
- MVPではコード署名証明書を使用せず未署名で配布し、SmartScreen警告の可能性を事前案内し、
  将来の署名追加を妨げない構成にすることを定めた。
- 「この端末の管理データを削除」はカタログ、復旧、一時、ログ、実行時情報だけを対象とし、
  プロジェクト、バックアップ、ごみ箱を削除しないことを定めた。
- 端末管理データは対象確認後に削除してアプリを終了し、次回は空の一覧で起動すること、
  プロジェクト本体はごみ箱移動と完全削除だけで削除することを定めた。
- ログはUTF-8、1ファイル10MB、現行＋過去5世代、最大約60MBとし、古い世代から
  削除することを定めた。
- ログには個人情報、絶対パス、セッション情報、編集履歴を記録せず、ログ書き込み失敗だけを
  理由に編集データを失わないことを定めた。
- プロジェクト名は必須・最大50文字・前後の半角全角空白を除去し、空文字、改行、制御文字を
  拒否して同名を許可し、UUIDで識別することを定めた。
- 同名は更新日時と保存場所末尾で区別し、プロジェクト名を内部ファイル・フォルダ名へ直接
  使用せず、絶対パス全体をブラウザへ返さないことを定めた。
- 新規プロジェクトは選択した親フォルダ直下の`hakamap-project-<project-uuid>`へ作成し、
  表示名変更ではフォルダ名を変更せず、既存フォルダを上書きしないことを定めた。
- 手動移動・改名後は再関連付け可能とし、ごみ箱内のフォルダ名にはUUIDだけを使用することを定めた。
- 背景は100MB・1億画素・各辺30,000px、添付は25MB・4,000万画素・各辺15,000pxを
  上限とし、プロジェクト全体は固定上限なしとすることを定めた。
- 上限超過時は自動縮小せず拒否し、拡張子、MIMEタイプ、実内容、空き容量を検証することを定めた。
- 添付サムネイルは240×180px枠、縦横比維持・拡大なし、WebP品質80とし、アセットSHA-256を
  基準に端末キャッシュへ保存することを定めた。
- サムネイルキャッシュはプロジェクト、バックアップ、エクスポートに含めず、端末管理データ
  削除の対象に含めることを定めた。
- サムネイルキャッシュは最大500MBとし、表示中を除く最終利用の古いものから削除し、
  元アセットがないものは起動時の清掃候補とすることを定めた。
- 生成失敗時は汎用アイコンを表示し、設定画面からキャッシュだけを削除でき、必要時に
  自動再生成することを定めた。
- 文字数上限はエリア名25、管理番号25、墓所名50、墓所備考1,000、添付表示名100、
  添付説明500、元ファイル名255文字とすることを定めた。
- 文字数はUnicodeコードポイント単位で数え、上限超過を切り捨てず、制御文字を拒否し、
  墓所備考と添付説明だけ改行を許可することを定めた。
- 人物のMVP項目は氏名、戒名、登録日時、更新日時とし、氏名か戒名の一方を必須、各50文字、
  重複可、前後空白除去、改行・制御文字不可とすることを定めた。
- 人物変更時は人物と所属墓所の更新日時を更新し、組名は墓所単位の将来拡張とすることを定めた。
- MVP検索は単一キーワードでエリア名、管理番号、墓所名、人物の氏名・戒名、添付表示名・
  説明を部分一致検索し、墓所備考とファイル内容は対象外とすることを定めた。
- 検索時は英字大小、全半角、Unicode空白、ひらがな・カタカナを同一視し、墓所UUIDで
  結果を重複排除することを定めた。
- 数値・日付範囲、項目別条件、AND／OR指定は将来拡張とした。
- 検索結果はエリア表示順、管理番号の自然順、墓所名、UUIDで安定ソートし、未割当は
  全エリアの後、未採番は同一エリアの採番済み墓所の後に表示することを定めた。
- 検索結果の並びは保存データを変更せず、MVPでは利用者による切り替えを設けないことを定めた。
- エリア表示順は作成順で末尾追加、名称変更では維持、削除時は詰め、Undo時は元位置へ戻し、
  インポート・エクスポート・バックアップ・復元で維持することを定めた。
- エリアの手動並べ替えはMVP対象外とし、将来拡張候補へ追加した。
- 性能基準は5,000墓所・20,000人（平均4人）とし、人物数に登録を禁止する固定上限を
  設けないことを定めた。
- 人物一覧は仮想スクロールを使い、検索データはJava側で墓所単位に集約して、人物全件を
  Reactへ常時送信しないことを定めた。
- 人物表示順は墓所内の登録順で末尾追加、編集では維持、削除時は詰め、Undo時は元位置へ戻し、
  インポート・エクスポート・バックアップ・復元で維持することを定めた。
- 人物の手動並べ替えはMVP対象外とし、将来拡張候補へ追加した。
- 添付は新規追加時に末尾へ配置し、ドラッグ＆ドロップまたは「上へ」「下へ」で並べ替え、
  並べ替えをUndo／Redo対象とすることを定めた。
- 複数追加時は選択ダイアログの順序を使い、削除時は詰め、Undo時は元位置へ戻し、
  `displayOrder`をインポート・エクスポート・バックアップ・復元で維持することを定めた。
- エクスポートは`.hakamap`拡張子のZIPとし、マニフェスト、プロジェクトJSON、アセットを含め、
  バックアップ、ロック、復旧、一時、キャッシュ、ログを除外することを定めた。
- マニフェストへ形式・アプリバージョン、作成日時、プロジェクトUUID、各ファイルのパス・
  サイズ・SHA-256を保持し、インポート時にZIPパスと全整合性を検証することを定めた。
- 未保存変更がある場合は保存成功後だけエクスポートし、古い保存状態だけの出力は設けず、
  保存失敗または切断時は開始しないことを定めた。
- エクスポート自体は更新日時、編集リビジョン、Undo／Redo履歴を変更せず、成功後も
  現在のプロジェクトを開いた状態にすることを定めた。
- インポート前は現在のプロジェクトの未保存変更を保存または破棄して閉じ、ロックと履歴を
  解放し、通常インポートではプロジェクトと配下のUUID・名称・日時を維持することを定めた。
- 同一UUIDは拒否または別プロジェクト化し、成功後は取り込み先を開き、失敗時は不完全な
  フォルダを残さず一覧へ戻り、デフォルト設定を変更しないことを定めた。
- インポートは選択先の`.hakamap-import-<random-uuid>.tmp`へストリーミング展開し、絶対・
  親参照パス、リンク、パス重複、未宣言ファイル、宣言サイズ超過を拒否することを定めた。
- 全検証後の正式名への変更を確定点とし、成功後だけカタログ登録し、削除できない残骸は
  隔離して次回起動時にも清掃することを定めた。
- インポート防御上限はマニフェスト10MB、プロジェクトJSON 100MB、100,010ファイル、
  圧縮率1,000倍、展開後合計＋500MBの空き容量とすることを定めた。
- 展開中に宣言サイズを超えた場合は中止し、ZIPを再帰展開せず、防御上限は通常編集の
  プロジェクト全体容量には適用しないことを定めた。
- 100,010ファイルは性能保証ではなく限界試験とし、通常性能試験は5,000墓所、平均2添付、
  約10,000添付を代表値として、ウイルス対策有効なWindows 11実機で所要時間を測定する。
- Windows 11標準のEdgeを基準とし、Edge、Chrome、Firefoxのリリース時最新安定版と
  1世代前を動作保証対象とすることを定めた。
- その他の既定ブラウザも起動を試み、必須機能が不足する場合は対応ブラウザを案内し、
  ブラウザ固有のファイルシステムAPIへ依存しないことを定めた。
- 人物単独のCSV／Excel一括入出力はMVP対象外とし、画面から1件ずつ管理し、プロジェクト
  全体の`.hakamap`には人物を含めることを定めた。
- CSVテンプレート、文字コード、検証エラー、重複判定、追加・更新ルールは将来拡張へ移した。
- 印刷・PDFはA4／A3、縦横選択、初期A4横、余白10mm固定とし、現在範囲または選択エリアを
  縦横比維持で用紙内へ自動縮尺することを定めた。
- 実寸縮尺、ヘッダー、フッター、ページ番号はMVP対象外とし、印刷とPDFで共通レイアウトを
  使用して出力前にプレビューすることを定めた。
- PDFは選択した用紙設定を反映した1ページ、画像は同じ用紙設定を反映した白背景のPNG 1枚とし、
  300 DPI相当の固定解像度で共通プレビューから出力することを定めた。
- Windowsの製品名と発行元を`Hakamap`、説明を「墓地マップ管理アプリ」、バージョンを
  3要素形式、インストーラー名を`Hakamap-<バージョン>.exe`とすることを定めた。
- アプリとインストーラーで16～256ピクセルを含む同じ`.ico`を使用し、正式版まで仮アイコンを
  差し替え可能にすること、およびアップグレード用製品識別子を固定することを定めた。
- 入力項目とファイル容量の上限を未確定へ変更した。
- 添付PDFを1ページ限定とし、複数ページを拒否して画像としてプレビューする仕様へ変更した。
- 管理番号を墓所の完成条件とし、未採番および情報未完成の墓所を保存可能とした。
- 通路、入口、建物、および自由地図要素をMVP対象外へ変更した。
- 拡張項目、マスターデータ、およびプロジェクト間設定コピーを将来候補へ移動した。
- 詳細検索仕様と人物入力項目を未決または将来候補へ移動した。
- 背景とバックアップに関する未確認の設計判断を設計候補へ移動した。
- 即時削除、手動保存、変更破棄、およびUndoの関係を確定した。
- Phase 3レビュー指摘を受け、`COMMIT_OUTCOME_UNKNOWN`では正式配置済みアセット、
  一時JSON、ステージング元、および復旧ファイルを結果確定まで保持するよう修正した。
- 結果確定時は、新JSONなら配置済みアセットを維持してコミット後清掃を行い、
  旧JSONなら配置済みアセットと一時JSONを清掃して編集を再開するよう修正した。
- コマンドは評価前に`expectedRevision`を検証し、セッション本体から切り離した
  Project集約コピーだけを評価へ渡すことで、評価中の直接変更が編集状態へ漏れないようにした。
- JSON置換直後の読戻し障害でも参照アセットを維持するテスト、古いリビジョンでは
  コマンドを評価しないテスト、および評価用集約の変更が本体へ漏れないテストを追加した。
- Phase 3レビュー指摘修正版コミット`4ce711886294770bf21bd12d0931ffe4dec597c8`は
  外部ChatGPTの再レビューで合格となった。
- Phase 4として、Spring Bootを`127.0.0.1`のOS選択ランダムポートへ限定し、
  `%LOCALAPPDATA%\HakamapData\runtime`のapplication lock、instance情報、異常終了マーカー、
  5秒間隔の生存更新、および正常終了時清掃を実装した。
- 二重起動時はPID、インスタンスUUID、256ビット相当の制御トークン、および既存プロセスの
  固有応答を確認し、最大10秒再試行して既存バックエンド側でブラウザを再表示するようにした。
- 起動用認証情報をURLフラグメントでReactへ渡し、URLから除去してから専用ヘッダーで
  一度だけ交換し、HttpOnly・SameSite StrictのセッションCookieを確立するようにした。
- `GET /api/v1/session`からCSRFトークンをno-storeで取得し、Reactのメモリだけに保持して
  状態変更要求へ付与し、認証・CSRF失効時は自動再送せず破棄するようにした。
- API境界でloopback、Host、Origin、Sec-Fetch-Site、Referer、セッション、およびCSRFを検証し、
  外部Origin、LAN、未認証、旧セッション、旧CSRF、偽装再表示要求を拒否するようにした。
- Problem Detailsの安全な固定コード、アクセスログ無効化、Webログ抑制、および
  例外値・認証情報・ポートを通常ログへ含めない設定を実装した。
- 保護された終了APIとReactの「Hakamapを終了」操作を追加し、受付後にSpringコンテキスト、
  セッション、ランタイム情報、およびapplication lockを正常に閉じるようにした。
- Phase 5として、Catalog一覧、新規Project作成、既存Project登録、open、close、
  デフォルト設定、再関連付け、および登録解除のAPIを実装した。
- Projectを同じストレージ内の`.hakamap-trash/<project UUID>`へ原子的に移動し、
  元の場所または指定場所への復元、明示的な完全削除を実装した。
- Project open中は`.hakamap.lock`を保持して同時に1件だけ開き、close時にロック、
  編集対象、およびセッションに紐づく一時ファイル選択を破棄するようにした。
- Catalog更新をPhase 3のCatalog保存トランザクションへ接続し、更新前の正常な
  `catalog.json`を1世代バックアップしてから原子的に確定するようにした。
- Swing `JFileChooser`をEDTで実行し、単一ファイル、複数ファイル、フォルダ、
  用途の組み合わせ、5分期限、セッション・用途拘束、単一使用、明示無効化、
  キャンセル、および多重ダイアログ拒否を実装した。
- ファイル選択結果とProject一覧は末尾の表示名だけを返し、絶対パスをAPIへ
  露出しないようにした。
- Reactの初期画面をProject管理画面へ変更し、新規作成、既存登録、open、
  デフォルト起動、ごみ箱、復元、完全削除、再関連付け、および登録解除を接続した。
- Project一覧へ戻る際は保存、破棄、キャンセルを明示的に選ぶ確認画面を追加した。
- ファイル選択の用途・セッション・期限・単一使用と、Project作成からopen／close、
  ごみ箱、復元、完全削除までを一時ディレクトリで確認する回帰テストを追加した。
- Phase 6として、人物全件を含まないProjectスナップショットと、全23種類の
  CommandTypeを専用sealed payloadへ変換する編集コマンドAPIを実装した。
- エリア、墓所、人物、背景、および添付の作成・変更・削除を候補Projectから確定差分へ変換し、
  `expectedRevision`確認後にUndo／Redo可能な1コマンドとして原子的に適用するようにした。
- 実質的な変更がない操作はrevision・履歴を増やさず`noChange`を返し、未割当・
  エリア範囲外の新規警告は5分有効・単一使用・セッション拘束の確認トークンで確定するようにした。
- 一括採番を、Projectとrevisionに紐づく5分有効・単一使用のプレビュートークンへ接続した。
- 人物一覧を墓所単位100件で返し、Project、墓所、revision、セッション、および5分期限へ
  サーバー側で紐づく不透明カーソルを実装した。
- PNG、JPEG、WebP、および1ページPDFを検証して管理アセットへ取り込み、PDFは
  Apache PDFBox 3.0.8でPNGへ変換するようにした。複数ページPDFと上限超過を拒否する。
- アセットUUIDから管理領域内の通常ファイルだけを解決し、固定ファイル名、
  `nosniff`、CSP、および`no-store`を付けて配信するAPIを実装した。
- 編集API処理を直列化し、同時要求が同じrevisionから候補評価・アセット配置を
  並行実行しないようにした。
- 全CommandTypeの未知項目拒否を含むDTO変換、正常差分、no-op、警告確認、
  revision競合、人物ページング、添付取り込み、およびPDF変換の回帰テストを追加した。
- Phase 6レビューのP1指摘を受け、Projectの「保存して閉じる」をPhase 3の
  `ProjectStorageTransactionCoordinator`へ接続し、保存成功後だけロックと編集セッションを
  解放するようにした。保存失敗・結果不明時はProjectを開いたまま維持する。
- 警告確認候補へ確定差分とCommand固有結果を一緒に保持し、確認後も単体・行列・範囲生成の
  `clientRef`と作成UUIDの対応を返すようにした。
- 背景・添付の新規ファイルをProjectの正式`assets`配下へ直接置かず、端末内の
  `temporary-assets/<project UUID>`へ置くProject単位ステージング管理を実装した。
- ステージングアセットはUndo／Redo履歴中も維持し、保存時にPhase 3の保存トランザクションで
  正式配置してからJSONを確定する。変更破棄時は一時実体を削除する。
- Recovery JSON用にステージングアセットの相対パス・容量・SHA-256を出力し、異常終了後に
  検証済みRecoveryスナップショットからProject単位のステージング参照を再構築できるようにした。
- 未保存アセットのプレビュー配信はステージング管理からUUIDで解決し、保存後は正式アセットを
  同じAPIから解決するようにした。
- 複数PDF添付の準備ループ全体を清掃対象とし、後続ファイルの検証失敗時も、それ以前に生成した
  変換PNGと空の変換ディレクトリを削除するようにした。
- 保存して閉じた後の再open、保存失敗時のopen維持、警告付き単体・行列・範囲生成の
  `clientRef`、添付・背景のステージング、Undo／Redo、正式保存、破棄、および
  異常終了後のステージング再構築、複数PDF途中失敗の回帰テストを追加した。
- Phase 6再レビューのP1指摘を受け、`RecoverySnapshotService`を本番Beanと30秒周期の
  スケジューラーへ接続し、開いているdirtyな編集セッションとステージングアセットを
  Project単位のRecovery JSONへ保存するようにした。
- 起動時のCatalog取得で有効な復旧候補を検出し、Projectを開く応答で復旧日時などを返す。
  Reactでは候補を表示し、利用者が復旧または破棄を選ぶまで編集画面へ移動しないようにした。
- 復旧時はrevision 0・空履歴・dirtyの編集セッションに加え、検証済みRecovery JSONの
  ステージング参照を`ProjectAssetStaging`へ復元する。保存・破棄後はRecovery JSONと
  不要なステージングを清掃するようにした。
- 「添付追加、未保存Recovery作成、実行時コンポーネント再生成、候補検証、復旧、
  添付表示、正式保存」を一続きで確認する結合テストを追加した。
- Phase 6再レビューで判明した再起動後の復旧破棄漏れを修正した。破棄時はRecovery JSONを
  検証してステージング参照を再構築し、一時アセットをすべて削除した後だけRecovery JSONを
  削除する。物理削除に失敗した場合はRecovery JSONを維持して失敗を返すようにした。
- 「未保存添付、Recovery作成、実行時コンポーネント再生成、候補検出、破棄」を通し、
  Recovery JSON、ステージングファイル、およびProject一時ディレクトリがすべて消える
  回帰テストを追加した。
- 複数アセットの破棄で途中失敗すると、次回の完全性検証が削除済みファイルを欠落扱いして
  再試行できない問題を補正した。復旧適用の完全性検証と破棄用の安全なパス検証を分離し、
  Project一時領域・アセットUUID・許可拡張子を確認したパスを`deleteIfExists`で冪等に
  削除する。全対象の不在確認後だけRecovery JSONを削除するようにした。
- 2件目の削除を初回だけ失敗させるファイル操作ラッパーによる障害注入テストを追加し、
  1回目はRecovery JSONを維持し、再試行で全一時ファイル、Project一時ディレクトリ、
  およびRecovery JSONを削除できることを確認した。
- `04bb24a`から`dbb616f`までの差分再レビューで指摘なしとなり、復旧破棄の
  冪等性、パス制約、Recovery JSONの削除順序、障害注入テスト、および文書整合性が
  確認された。Phase 0～6は最終合格となった。
- Phase 7として、左サイドバー、中央地図領域、右プロパティパネルの編集シェルを実装し、
  低解像度時の自動折りたたみとツールバーからの左右パネル切り替えを接続した。
- Projectスナップショット、人物一覧、履歴をTanStack Queryで管理し、Zustandには
  選択墓所、表示タブ、およびパネル状態だけを保持して業務状態を重複させないようにした。
- 左サイドバーへ全墓所、未割当、未採番、情報未完成の件数と墓所一覧を表示し、
  右パネルへ基本情報、人物、添付、履歴の4タブを実装した。
- 墓所基本情報の適用、人物追加・削除、添付追加・削除・プレビュー、Undo／Redoを
  Phase 6のAPIへ接続し、成功後はAPI確定値を再取得するようにした。
- 未適用フォームを保持した選択変更の破棄確認、revision競合時の入力保持と最新情報取得、
  通信失敗通知、処理中表示、キーボードフォーカス、およびパネル折りたたみを実装した。
- Projectを閉じずに保存するAPIとツールバーの手動保存を追加し、保存後も編集セッションと
  Undo／Redo履歴を維持するようにした。
- 編集画面の3領域・4タブと、未適用入力がある選択変更の確認をフロントエンドテストへ追加した。
- Phase 8として、PixiJSを専用アダプターへ閉じ込め、背景、エリア、墓所、
  オーバーレイのレイヤーとviewport、座標変換、およびGPUリソース解放を実装した。
- 墓所優先クリック、Ctrl複数選択、四方向矩形選択、エリア編集、墓所・エリア作成、
  複数移動、サイズ変更、背景変形、および矢印キー移動を編集APIへ接続した。
- 10%～800%のポインター中心ズーム、全体表示、8pxスナップ、ガイド、
  重なり候補表示、およびJava拒否時の確定状態再取得を実装した。
- 背景を最大1024pxの縮小タイル階層へ分割してSHA-256単位の端末キャッシュへ保存し、
  viewport周辺だけをPixiJSへ読み込んで画面外テクスチャを解放するようにした。
- 座標往復、負座標、ズーム境界、ポインター中心ズーム、当たり判定、矩形選択、
  接触と重なり、ズーム別8pxスナップ、および5,000墓所の途中性能テストを追加した。
- Phase 8レビューのP1指摘に対し、WebP対応ImageIOデコーダーを追加してWebP実ファイルの
  タイル生成を検証し、回転背景では画面四隅を背景ローカル座標へ逆変換するよう修正した。
- 墓所ラベルを管理番号、墓所名、両方、非表示から切り替えてPixiJSへ描画し、
  低ズーム時と画面外ではText生成を抑止するようにした。
- 全体表示へ回転・X/Y拡縮後の背景外接矩形を含め、コマンド処理中はPixiJS操作、
  矢印キー移動、および背景数値入力を開始できないようにした。
- Phase 8レビュー指摘修正版の再レビューでは新たな指摘がなく、WebP、回転背景、
  墓所ラベル、全体表示、および処理中操作停止の修正が確認され、Phase 7・8は合格となった。
- Phase 9として、Java側で標準項目、人物、添付を正規化部分一致検索し、自然順に並べた
  墓所結果を不透明カーソルで200件ずつ返すAPIを実装した。備考は検索対象に含めない。
- 左サイドバーへ検索結果一覧を保持し、最初の結果へズームし、結果クリックで該当墓所を
  選択して中央表示するようにした。検索はProjectのrevision、dirty、履歴を変更しない。
- 人物一覧を100件ずつの無限取得へ変更し、直近5墓所だけをキャッシュして固定行高で
  仮想表示するようにした。人物の追加・編集・削除は確定後にキャッシュを再取得する。
- 添付の追加・削除に加えて上下並べ替えを実装し、画像要素を使うダイアログプレビューへ
  統一した。PDFは取込時の変換画像だけを表示し、PDFビューアーや能動コンテンツを実行しない。
- 検索の文字正規化、人物項目、備考除外、管理番号の自然順、および検索前後の
  revision・dirty不変をバックエンドテストへ追加した。
- Phase 10として、手動保存成功後の1日1回・3世代自動バックアップ、バックアップ一覧、
  不透明候補ID・版、復元前退避、および差し替え検知付き復元を実装した。
- Project JSONとアセットをSHA-256マニフェスト付きZIPへストリーミング格納する共通処理を
  自動バックアップ、`.hakamap`エクスポート・インポート、および復元で共有した。
- ZIPパストラバーサル、大小文字重複、未宣言ファイル、サイズ・SHA-256不一致、
  ファイル数・圧縮率上限を拒否し、展開後にJSON Schemaと業務整合性を再検証する。
- エクスポート、インポート、復元をHTTP 202のOperationへ接続し、セッション境界、
  約500ミリ秒の状態取得、確定処理前の協調キャンセル、および完了後10分保持を実装した。
- A4／A3、縦／横、現在表示範囲／選択エリアのプレビューと、編集表示を除外した
  300 DPI相当PNGおよびブラウザ印刷・1ページPDF出力を実装した。
- Phase 10レビュー指摘に対応し、復元対象を一時領域へ完全構築したうえで
  Projectディレクトリ全体を切り替え、再読込失敗時は元データとロックへ戻すようにした。
  復元元にない旧アセットは完全置換で除外する。
- アーカイブへ20GiBの非圧縮合計上限、ストリーム実測上限、および空き容量検査を追加した。
- 同一ProjectのOperation予約、キュー実行直前のrevision再検証、想定外例外の固定コード化、
  検査・圧縮・展開中の協調キャンセル、および確定開始後のキャンセル拒否を実装した。
- Projectディレクトリの兄弟に安定ロックを配置して復元交換中も保持し、別プロセスによる
  介入を拒否するようにした。アセットの事前ハッシュ、復元前退避、および既存バックアップの
  引継ぎも協調キャンセル対象とし、引継ぎ容量を復元前の空き容量検査へ含めた。
- 復元開始時の候補アーカイブ再検証にもOperationのキャンセル制御を伝播し、
  アーカイブ全体と各エントリーのSHA-256検証中も64KiB単位で中断可能にした。
- Phase 11として、Java 21の`jlink`ランタイムとユーザー単位`jpackage` EXEを生成する
  Windows PowerShellスクリプトを追加した。固定アップグレードUUID、デスクトップ・
  スタートメニューショートカット、512MiB heap、およびSHA-256 Manifestを設定した。
- 配布設定の自動検査、Windows実機結果JSONの雛形、ローカル・外付け・SMB 3.x向けの
  排他ロック・置換・同期読戻し・清掃の保存先機能検査を追加した。
- Windows CIで同じビルドスクリプトからEXEを作成し、ManifestのSHA-256を照合して
  7日間だけ成果物を保存するWorkflowを追加した。
- Linuxで同じモジュールの`jlink`ランタイムを生成した。初回試行で不足した
  `java.instrument`を検出して追加し、Spring BootとローカルHTTPの初期化まで確認した。
- Windows 11実機へMicrosoft OpenJDK 21.0.11とWiX Toolset 3.14を導入し、
  `java`、`jlink`、`jpackage`、`candle`、`light`を実行できることを確認した。
- Windows PowerShell 5.1がBOMなしUTF-8の日本語を誤認してビルドスクリプトを解析できない
  問題を検出し、Windows用PowerShellスクリプトをUTF-8 BOM付きとして互換性を確保した。
- Windows PowerShell 5.1では`java -version`の正常な標準エラー出力が停止対象になるため、
  バージョン検査を標準出力へ出す`java --version`へ変更した。
- WindowsのGitによるCRLF変換でPrettier検査が全件失敗する問題を検出し、
  `.gitattributes`で通常テキストをLF、WindowsバッチをCRLFへ固定した。
- Windowsのクリーンコンパイルで復元バックアップ容量集計の`IOException`処理漏れを検出し、
  復元処理の既存例外変換境界へ容量集計とアーカイブ展開を含めた。
- WindowsでMaven標準検査が成功した後、作業領域初期化が生成済み仮アイコンを削除して
  `jpackage`が失敗する問題を検出し、初期化後に仮アイコンを生成する順序へ修正した。
- Windows EXEのインストール、ユーザー単位配置、デスクトップ・スタートメニューの
  ショートカット作成、および配布物のSHA-256一致を実機で確認した。
- 同梱JVMとSpring Bootは起動したが、縮小ランタイムでAWT `Desktop`が利用不可となり
  ブラウザー起動例外で停止する問題を検出した。Windowsでは引数を分離した標準URLハンドラーを
  フォールバックとして使用するようにした。
- 修正版を0.1.1として上書きインストールし、ブラウザー表示、プロセスを増やさない二重起動、
  タブを閉じた後の再表示、および画面内終了後のプロセス0件を実機で確認した。
- Windows 11推奨環境の起動3回は2.838秒、2.834秒、2.712秒で、
  中央値2.834秒、最大2.838秒となり5秒基準に合格した。
- 同環境の通常起動時ワーキングセットはHakamap 198.0MiB、Chrome 771.6MiB
  （GPU 100.8MiBを含む）、合計969.5MiBで1GiB基準に合格した。
  最大背景・5,000墓所を読み込んだ負荷時メモリは未測定である。
- ローカル保存先機能検査で、Windows PowerShell 5.1が`File.Replace`の`$null`
  バックアップパスを空文字列として渡す問題を検出した。検査用バックアップファイルを
  明示して置換後に削除する方式へ修正した。再検査では排他ロック、置換、同期書込み・
  読戻し、および検査領域清掃がすべて成功し、ローカル保存先は合格した。
- Windows画面からProject追加を開始するとファイル選択APIが
  `file-selection-unavailable`となる問題を検出した。Spring Boot既定のヘッドレスモードが
  Swingファイル選択とAWT連携を無効化していたため、デスクトップアプリ起動時に
  `setHeadless(false)`を明示するよう修正した。Windows EXEでの再確認待ちである。
- Windowsで作成・保存・再読込したエリアと墓所、および墓所の標準項目が維持されることを
  確認した。一方、再読込直後の地図表示が固定のパン位置となり、要件上の初回全体表示を
  実行していない問題を検出した。PixiJS初期化完了時に背景・エリア・墓所へ
  `fit()`するよう修正した。小さな単一要素は800%上限まで拡大表示されるが保存寸法は変えない。
- 0.1.2から0.1.3への上書き更新でProject一覧が消失した。Project本体の`project.json`と
  アセットは利用者指定先に残っており、一覧用`catalog.json`だけが失われた。
  原因はjpackageのインストール先とカタログ・復旧・一時データの保存先がともに
  `%LOCALAPPDATA%\Hakamap`だったことである。アプリデータルートを兄弟ディレクトリの
  `%LOCALAPPDATA%\HakamapData`へ分離し、パス回帰テストと関連設計を更新した。
  0.1.4で残存Projectを再登録し、0.1.5への上書き更新後も一覧、エリア、墓所、管理番号、
  墓所名、および備考が維持されることを確認した。
- 0.1.5では、Projectを開いた直後の全体表示と、ヘッドレスモード解除後のファイル選択
  ダイアログも実機で再確認し、いずれも合格した。
- Windows実機で、PixiJS地図を拡大するとエリア名と墓所ラベルがぼやける問題を検出した。
  ズームに反比例する極小フォントのテクスチャを拡大する方式をやめ、通常サイズの文字を
  生成してText要素のスケールだけを逆補正し、画面上の大きさと解像度を維持するよう修正した。
- Chromeが日本語UIを自動翻訳して表示を崩す問題に対し、HTMLの言語を日本語に修正し、
  `translate=no`、`notranslate`、Google翻訳抑止メタデータを設定した。
- 0.1.6のWindows実機確認では、拡大時もエリア名と墓所ラベルが鮮明に表示され、
  Chrome自動翻訳が起動せず、タブタイトルが`Hakamap`となることを確認した。
- Windows実機でネットワーク切断中の起動、Project編集・保存、検索、出力確認、および終了を
  完遂し、オフライン動作を確認した。
- 画面内終了後にブラウザタブが残る点へ、自動クローズ要求と終了済み案内を追加した。
  ブラウザがスクリプトによるタブ終了を拒否した場合は、利用者へタブを閉じるよう案内する。
- Windowsファイル選択画面へ最前面の所有ウィンドウを設定し、選択中はWeb画面をモーダルで
  操作不能にして、Windows側で選択またはキャンセルする旨を表示するよう修正した。
- Windows実機で、画面内終了後の終了済み案内と、ファイル選択ダイアログの最前面表示・
  選択中のWeb画面操作抑止を確認し、いずれも合格した。
- Windows表示倍率150%でPixiJS地図内の文字だけがぼやける問題を検出した。端末の
  `devicePixelRatio`をキャンバスと文字テクスチャへ反映し、描画負荷を制限するため
  解像度を1～2倍へ収めるよう修正した。Windows実機の表示倍率150%で文字と画面操作を
  再確認し、合格した。
- 共通メニューへ「操作ガイド」を追加し、Undo／Redo、拡大・縮小・全体表示、パン、選択、
  墓所移動、編集モード、保存、ファイル選択、および終了・再表示の操作を画面内で確認できる
  ようにした。
- Undo／Redoはプロジェクト全体の直前のデータ変更へ作用するため、墓所プロパティ内の操作から
  地図上部の常時表示ツールバーへ移した。墓所未選択でも実行でき、履歴タブは一覧確認に限定する。
  MVPでは`Ctrl+Z`および`Ctrl+Y`を割り当てない。
- Windows実機で共通メニューの操作ガイド表示と、墓所未選択状態を含む地図上部の
  Undo／Redo操作を確認し、いずれも合格した。
- 左右パネルを閉じてもPixiJSキャンバスが以前の寸法に留まる問題へ、中央領域を監視する
  `ResizeObserver`を追加し、パネル開閉のアニメーション中も利用可能領域へ追従させた。
- 閉じた右プロパティパネルを、同じ墓所の再選択を含む墓所選択時に自動表示するよう修正した。
- Windows実機の1366×768表示で、左右パネル開閉時の地図全面追従と、墓所選択時の
  右プロパティパネル自動再表示を確認し、最低解像度の主要画面操作は合格した。
- Windows実機で地図内文字が小さく読みにくい点を検出した。エリア名と墓所ラベルを
  画面上14px、日本語向けローカルフォント、セミボールド、白い縁取りへ統一した。
- Windows実機には検証用のUSBメモリまたは外付けドライブがないため、外付け媒体の
  保存・切断試験は未実施として残す。ローカル保存先の合格結果では代替しない。
- 利用可能なNAS、共有サーバー、または別PCのWindows共有フォルダもないため、SMB 3.xの
  保存・排他・切断試験は未実施として残す。
- 墓所基本情報の適用時、未入力の備考が空文字として送信され、任意項目の値オブジェクト
  生成に失敗する問題を修正した。API境界でUnicode空白だけの任意文字列を未設定として扱い、
  管理番号と墓所名だけを入力した更新の回帰テストを追加した。
- 2件目以降のエリア作成で画面が常に青色を指定し、エリア色の一意制約により拒否される
  問題を修正した。作成時は色を未指定とし、バックエンドが次の未使用色を選択する。
- バックエンドには存在していた背景取込が画面へ接続されていなかったため、地図ツールバーへ
  背景の追加・差し替え・削除を追加した。差し替えでは現在の配置を引き継ぎ、削除前は
  エリアと墓所の位置を維持してUndo可能であることを確認表示する。
- エリア編集へ右下ハンドルによるサイズ変更、選択エリアの名称入力、および移動時の
  墓所・他エリアへのスナップを追加した。墓所をエリア端へ合わせる場合は内側の閉境界を
  優先し、実際に範囲外へ配置する場合は警告確認後に確定できるようAPIを接続した。
- APIエラー時にHTTP状態だけでなくProblem Detailsの安定エラーコードを取得し、重複、
  重なり、入力上限などの具体的な原因と対処を日本語表示するようにした。操作通知は
  画面上部中央のSnackbarへ統一し、エラーは赤色で5秒後に自動消去する。
- Windows実機確認を受け、ファイル選択をAWTのネイティブダイアログへ変更し、背景取込後は
  自動で背景全体へ表示を合わせるようにした。配置警告は物理名を日本語へ変換し、Snackbarの
  終了時に別色が一瞬表示される問題を除去し、「選択」を「墓所編集」へ変更した。
- ネイティブファイル選択がブラウザの背後へ表示される実機結果を受け、所有ウィンドウの
  フォーカス取得を待ってからファイルダイアログを開く二段階の前面化へ修正した。
- 背景選択後に画像が地図へ表示されない問題に対し、背景タイルAPIを`.png`で終わるURLへ
  統一し、PixiJSの画像読込時にもPNG形式を明示した。未保存の背景も既存の一時アセット
  解決経路からタイル化し、画像ローダーが拡張子のないAPI URLを判定できない問題を除去した。
- 背景配置の座標・回転・倍率入力を廃止し、画像ドラッグ、右下の拡縮ハンドル、上辺中央の
  回転ハンドルへ統一した。反時計回り操作も許容し、確定値は0～360度未満へ正規化する。
- ホイールズーム等で背景タイルの表示階層が変わる際、同じタイルを重複読込したり、読込中の
  テクスチャを二重解放したりしないよう、読込中状態と安全な解放処理を追加した。
- 背景回転を画像中央基準へ変更し、90度単位から5度以内の角度スナップと、X倍率を基準に
  元画像の縦横比へ戻す操作を追加した。Snackbarは独立タイマーで5秒後に閉じる。
- 初回の背景差し替えでもネイティブファイル選択を前面に保つため、ダイアログ表示直後に
  フォーカスを再取得するWindows向けの遅延処理を追加した。
- 墓所とエリアへ中心基準の回転ハンドルを追加し、90度単位から5度以内の角度スナップを
  適用した。描画、当たり判定、矩形選択、重なり、および所属範囲判定には回転後の外周を
  使用し、既存JSONでエリア回転がない場合は0度として読み込む。
- 「縦横比を戻す」は背景画像の中心位置を維持してX倍率へY倍率を揃えるようにした。
- Windows実機で、背景・エリア・墓所の中心回転、90度単位の角度スナップ、背景の
  縦横比復元、回転形状の重なり・辺接触・所属判定、保存後の再読込、および
  Undo／Redoを確認し、対象の回帰テスト12項目すべてに合格した。
- Windows実機の5,000墓所試験では保存448msで合格した一方、全墓所の図形・ラベルと
  左一覧5,000件の同時生成により画面操作が重くなった。画面外墓所の描画除外、墓所図形の
  単一Graphicsへの一括描画、表示中300件超でのラベル抑制、および左一覧200件単位の
  段階表示へ修正し、実機再試験待ちとした。
- 描画軽量化後は拡大・縮小、パン、墓所選択、およびプロパティ表示が合格したが、
  `open`は8.05秒で5秒基準を超過した。読込時の重なり検証を全組合せから、回転後の
  外接範囲をX座標順に走査して近接候補だけ精密判定する方式へ変更し、5,000件を2秒以内で
  検証する自動テストを追加した。Windows実機での`open`再測定待ちである。
- 同じ起動で`GET /api/v1/catalog/projects`も6.65秒を要した。復旧ファイルが存在しない
  Projectまで復旧候補判定のため本体を読み直していたため、復旧ファイルの存在確認を先に行い、
  候補が存在するProjectだけを詳細検証するよう変更した。`projects`も実機再測定待ちである。
- 0.1.5のアンインストールではアプリ本体とショートカットだけが削除され、分離後のCatalogと
  Project本体は維持された。再インストール後は再登録なしで一覧とProject内容を再利用でき、
  0.1.6への更新後も維持された。
- 外付け媒体とSMBは検証環境なしで未実施として残る。加えて、最低ハードウェア環境、
  最大背景、5,000墓所負荷、および障害系試験が残っているため、Phase 11は完了扱いにしていない。

## 次に行うこと

Windows側のリポジトリを更新し、`packaging/windows/README.md`に従ってEXEを作成して、
`docs/verification/phase-11-windows.md`の未実施項目を検証する。

1. `jlink`と`jpackage`によるWindows EXEを作成してショートカット・更新・削除を検証する。
2. Windows最低・推奨環境で起動、読込、検索、操作、保存、およびメモリを測定する。
3. 最大背景、外付け・NAS、切断、容量不足、オフライン、高DPI等の障害試験を実施する。

## 変更したファイル

- `backend/config/checkstyle/checkstyle.xml`
- `backend/pom.xml`
- `backend/src/main/java/jp/hakamap/infrastructure/persistence/schema/`
- `backend/src/main/java/jp/hakamap/infrastructure/fileselection/`
- `backend/src/main/java/jp/hakamap/persistence/json/`
- `backend/src/main/java/jp/hakamap/project/application/`
- `backend/src/main/java/jp/hakamap/project/application/editing/`
- `backend/src/main/java/jp/hakamap/project/domain/`
- `backend/src/main/java/jp/hakamap/project/infrastructure/`
- `backend/src/test/java/jp/hakamap/HakamapApplicationTests.java`
- `backend/src/test/java/jp/hakamap/infrastructure/persistence/schema/`
- `backend/src/test/java/jp/hakamap/infrastructure/fileselection/`
- `backend/src/test/java/jp/hakamap/persistence/json/`
- `backend/src/test/java/jp/hakamap/project/application/`
- `backend/src/test/java/jp/hakamap/project/application/editing/`
- `backend/src/test/java/jp/hakamap/project/domain/`
- `backend/src/test/java/jp/hakamap/project/infrastructure/`
- `frontend/package.json`
- `frontend/pnpm-lock.yaml`
- `frontend/src/`
- `frontend/vite.config.ts`
- `AGENTS.md`
- `README.md`
- `docs/development-handoff.md`
- `docs/implementation-plan.md`
- `docs/design/application-lifecycle.md`
- `docs/design/basic-architecture.md`
- `docs/design/backup-and-restore.md`
- `docs/design/data-storage-and-privacy.md`
- `docs/design/domain-model.md`
- `docs/design/json-schema-and-persistence-model.md`
- `docs/design/commands-and-history.md`
- `docs/design/storage-transactions.md`
- `docs/design/local-api.md`
- `docs/design/frontend-state-management.md`
- `docs/design/pixi-map-interaction.md`
- `docs/design/test-strategy.md`
- `docs/design/frontend-backend-integration.md`
- `docs/design/frontend-ui.md`
- `docs/design/local-http-boundary.md`
- `docs/design/map-background-import.md`
- `docs/design/non-functional-requirements.md`
- `docs/design/offline-operation.md`
- `docs/design/file-selection.md`
- `docs/design/print-and-pdf-output.md`
- `docs/design/search.md`
- `docs/design/undo-redo.md`
- `docs/design/windows-packaging.md`
- `docs/adr/0001-react-spring-browser-architecture.md`
- `docs/adr/0002-project-folder-json-storage.md`
- `docs/adr/0003-jpackage-windows-exe.md`
- `docs/requirements/README.md`
- `docs/requirements/assets.md`
- `docs/requirements/data-and-search.md`
- `docs/requirements/editor-ui.md`
- `docs/requirements/future-extensions.md`
- `docs/requirements/map-editing.md`
- `docs/requirements/output.md`
- `docs/requirements/product-requirements.md`
- `docs/requirements/project-management.md`

## 検査結果

- 今回の5,000墓所読込最適化後の`cd backend && ./mvnw verify`: 成功。
  フロントエンド54件、Java 138件、Checkstyle、Spotless、および実行可能JAR生成を含む。
- 背景・エリア・墓所回転対応後の`cd backend && ./mvnw verify`: 成功。
  フロントエンド54件、Java 137件、Checkstyle、Spotless、および実行可能JAR生成を含む。
- `cd backend && ./mvnw verify`: 成功。フロントエンド標準検査、127件のJavaテスト、
  Checkstyle、Spotless、および実行可能JAR生成を含む。
- 背景タイルURL修正後の`cd backend && ./mvnw verify`: 成功。フロントエンド42件、
  Java 133件、Checkstyle、Spotless、および実行可能JAR生成を含む。
- `cd frontend && pnpm lint`: 成功。
- `cd frontend && pnpm format:check`: 成功。
- `cd frontend && pnpm test`: 成功（21テスト）。
- `cd frontend && pnpm build`: 成功。
- 生成JARの`/`はHTTP 200、`/api/v1/not-defined`はHTTP 404となることを確認した。
- JAR内の`BOOT-INF/classes/static`にReactのHTML、JavaScript、CSSがあることを確認した。
- Maven依存ツリーにH2、Spring Data、およびFlywayが存在しないことを確認した。
- `backend/target`、`frontend/dist`、`frontend/node_modules`がGit除外対象であることを確認した。
- `git diff --check`: 成功。

## 注意事項

- 5,000墓所の重なり検証候補絞り込みと、復旧ファイルがないProjectの一覧取得時に
  本体再読込を省略するWindows実機再テスト基準コミットは`672c511`である。
- 5,000墓所の画面外描画除外、図形一括描画、密集時ラベル抑制、および左一覧の
  段階表示を含むWindows実機再テスト基準コミットは`ebffc7f`である。
- 要件への回答を受け取るたびに文書を更新する運用としている。
- 背景のドラッグ移動、ハンドルによる拡縮・回転、およびホイールズーム時のタイル読込競合
  修正を含むWindows実機テスト基準コミットは`2d48d0a`であり、`origin/main`へ反映済みである。
- 背景タイルを`.png`付きURLで配信し、PixiJS側でも画像形式を明示する修正の
  Windows実機テスト基準コミットは`13e113a`であり、`origin/main`へ反映済みである。
- ネイティブファイル選択の所有ウィンドウへフォーカスが移るのを待ってから表示する
  実機再テスト基準コミットは`bc23b4f`であり、`origin/main`へ反映済みである。
- Windowsネイティブファイル選択、背景の自動全体表示、配置警告の日本語化、Snackbarの
  終了表示、および「墓所編集」への名称変更を含む実機テスト基準コミットは`9242024`であり、
  `origin/main`へ反映済みである。
- 背景設定、エリア編集、墓所の内側境界スナップ、配置警告確認、および具体的な
  Snackbar通知までを含むWindows実機テスト基準コミットは`095f774`であり、
  `origin/main`へ反映済みである。
- 墓所の空欄任意項目と、2件目以降のエリア色選択に起因する適用失敗を修正した
  作業再開基準コミットは`e910a46`であり、`origin/main`へ反映済みである。
- Phase 11の地図ラベル視認性改善、最低解像度の実機確認、および検証環境がない
  外付け媒体・SMB試験の整理までを含む作業再開基準コミットは
  `9f93a4df1f55c66362c3f04820ff3905b99a6c9a`であり、`origin/main`へ反映済みである。
- Phase 11は未完了だが、Windowsホストから実機検証スクリプトを取得するため、
  検証用チェックポイント`e36e053`を作成した。Phase完了コミットとして扱わない。
- Phase 10レビュー指摘修正のレビュー基準コミットは
  `082fbb1d309f61137c2bd25b033ebf781f450f59`である。復元の原子切替とロールバック、
  旧アセット除去、アーカイブ総量・空き容量防御、Operation競合制御と例外秘匿、
  安定ロックの継続保持、全ファイル処理区間の協調キャンセル、および
  既存バックアップを含む復元容量検査を含み、外部レビュー合格済みである。
- Phase 6の自動復旧実行時接続と部分失敗後も再試行可能な復旧破棄までを含む
  レビュー基準コミットは`bb4d272becdf1fbf0bd43c213ca28216a3bd06a5`であり、
  `origin/main`へ反映済みで、外部レビュー合格済みである。
- 詳細設計8工程、JSON Schema、および追加レビュー指摘反映までのレビュー基準コミットは
  `ca5f8b1`であり、
  `origin/main`へ反映済みである。
- 現在のプッシュ済みレビュー基準は
  `dbb616f8097a8e5c6296f8b0c6cc131f3a1060b0`である。Phase 0～6は
  外部ChatGPTレビュー合格済みで、`origin/main`へ反映済みである。
- Phase 7コミット`27836783a50e17d181b37e16e4172067b89e9389`とPhase 8コミットを
  `origin/main`へ反映済みである。Phase 7～8の外部レビュー基準コミットは
  `15d7b63221ca23b9e221abc9a3697ef347dba4df`である。
- Phase 8レビュー指摘の修正基準コミットは
  `6033bfd82db8b3eb2f6674da05b5e3585ff49fec`である。WebP背景、回転背景タイル範囲、
  墓所ラベル4モード、背景を含む全体表示、および処理中の操作停止を含む。
- Phase 2ではProject、Catalog、Recovery v1の保存record、Project Mapper、防御的JSON Codec、
  Schema後のJava整合性検証、およびファイルRepositoryを実装した。
- ProjectとRecoveryのアセットは相対パス、通常ファイル、シンボリックリンク拒否、
  UUIDファイル名、拡張子、容量、SHA-256、および画像シグネチャを検証する。
- Catalogはドライブ絶対パスとSMB共有のUNCパスを扱い、Windowsの大文字小文字を無視した
  正規化後にUUID、パス、およびデフォルトProjectの整合性を検証する。
- Phase 3では`.hakamap.lock`、保存処理の直列化、基準JSONのSHA-256照合、空き容量検査、
  ステージングアセットの同期・原子的配置、および検証済み一時JSONの原子的置換を実装した。
- 原子的置換後の読戻し不能を`COMMIT_OUTCOME_UNKNOWN`として編集停止し、再接続後に
  新旧SHA-256で確定・未確定・未解決を判定する。結果不明中は新JSONが参照する可能性のある
  アセットと清掃対象を保留し、確定結果に応じて維持または清掃する。
- ChangeSetは変更対象だけの前後差分を保持し、revision競合、最大100件、Undo後の分岐、
  保存後も維持するUndo／Redo、保存指紋によるdirty判定、および履歴用の非機密情報を実装した。
- コマンド実行では評価前後にrevisionを検証し、切り離したProject集約コピーで評価してから
  ChangeSetだけを現在の編集セッションへ適用する。
- Recoveryは未保存時のみ30秒間隔で原子的に更新し、正式Projectと基準SHA-256が一致する場合だけ、
  revision 0・空履歴・未保存状態の新しい編集セッションとして適用する。
- Catalogは更新前の正常JSONを`catalog.json.bak`へ原子的に確定してから、新しいCatalogを
  原子的に置換する。下位のJSON Repositoryも同期済み一時ファイルから原子的に置換する。
- Phase 3レビュー指摘修正版コミット`4ce711886294770bf21bd12d0931ffe4dec597c8`までを
  `origin/main`へプッシュし、`HEAD`と`origin/main`の一致を確認した。
- 本メモのコミット後に追加された変更がある場合は、作業終了時に引き継ぎメモを更新し、
  利用者へコミットとプッシュを提案する。
